/*
Task
Given an array arr of n integers. Write a function to return the number of odd elements in arr on the screen.

For example, if n = 7, arr = [2, 5, 3, 6, 9, 4, 1], enter the following:

7
2 5 3 6 9 4 1
When the code is compiled and executed, it produces the following result:

4
Because there are 4 odd numbers in arr.
*/
#include <stdio.h>
#include <math.h>
#include <string.h>
void max(int a[],int n)
{
	int cnt =0;
	for(int i=0;i<n;i++)
	{
       if(a[i]%2!=0)
	   {
		   cnt++;
	   }
	}
	printf("%d",cnt);
}
int main()
{
	int n;
	scanf("%d",&n);
	int a[100];
	for(int i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	max(a,n);
}
